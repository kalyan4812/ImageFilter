package com.example.workmanagerdemo


/*


***workmanger = (api from android jetpack) api to shecdule defferable,asychnronous tasks which run reliably.

defferable means= compatible with doze mode,power saving mode + scheduling mechanism(one time,repetative)

reliable means=always finishes the work+ run under constraints(like only wifi is there,device idle,charging,
sufficient storage).


types of tasks= immediate,long running,scheduled tasks for future.

** workmanger stores tasks in database internally,so that it will execute the tasks for sure.











 */